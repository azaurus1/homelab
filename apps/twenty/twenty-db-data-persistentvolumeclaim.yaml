apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: twenty-db-data
  name: twenty-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
