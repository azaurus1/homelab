apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: twenty-docker-data
  name: twenty-docker-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
