apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml convert -o ./kube
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: lifevisor-syncthing
  name: lifevisor-syncthing-gui
spec:
  ports:
    - name: "8384"
      port: 8384
      targetPort: 8384
  selector:
    io.kompose.service: lifevisor-syncthing
