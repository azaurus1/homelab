apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: desktop-activity-data
  name: desktop-activity-data
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 2Gi
