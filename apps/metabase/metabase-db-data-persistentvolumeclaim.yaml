apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: metabase-db-data
  name: metabase-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
